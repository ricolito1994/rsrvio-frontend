import React, {useEffect, useState, useContext} from 'react';
import { AppContext } from 'app/context';

const ListingsPage = (): React.ReactElement => {
     const {
            
    } = useContext<any>(AppContext);
    return (<>Listings</>)
}

export default ListingsPage;